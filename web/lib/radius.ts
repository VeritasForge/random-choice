/** 조회에 쓰는 기본 반경(m). 한 지점 주변에 45곳 넘게 있으면(도심 대부분이
 * 그렇다) 이 값은 결과에 영향을 주지 않는다 — 500m로 물어도 5km로 물어도
 * 똑같은 45곳이 온다(실측으로 확인됨). 45곳을 못 채우는 드문 지역에서만
 * 실제로 결과를 바꾼다. */
export const DEFAULT_RADIUS = 500;

/** 빈 결과일 때만 쓰는 넓힌 반경(m). 결과가 없다는 것은 정의상 한 지점
 * 주변이 45곳을 못 채우는 드문 지역이라는 뜻이므로, 이때는 반경을 넓히면
 * 실제로 더 먼 가게까지 받아 온다. */
export const WIDER_RADIUS = 2000;
