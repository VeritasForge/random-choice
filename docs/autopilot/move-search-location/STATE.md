# 상태 — 위치를 옮겨 찾기
- 모드: 만들기
- 목표: 설계 문서대로 위치를 옮겨 찾는 기능의 구현 계획을 세우고 구현까지 마친다
- 완료조건: (사용자가 /goal로 건 목표 원문) 구현 계획을 짜고 앞서 spec의 내용을 완성한다. 별도 worktree에서 진행하고 main에 머지하지 않는다. brainstorming → writing plan → subagent development 순서를 따른다. 판단의 갈림길을 기록으로 남긴다.
- 턴: 2 (완료)
- 작업 경로: `.claude/worktrees/move-search-location` (브랜치 `worktree-move-search-location`)
- 설계 문서: `docs/superpowers/specs/2026-09-20-move-search-location-design.md`
- 계획 파일: `docs/superpowers/plans/2026-09-20-move-search-location.md` (열한 개 작업, 전부 완료)

## 확정된 것
- [확정] 열한 개 작업이 모두 끝났고 작업마다 리뷰를 통과했다 — 근거: `.superpowers/sdd/2026-09-20-move-search-location/progress.md`의 `complete` 줄 열한 개
- [확정] 브랜치 전체 리뷰에서 나온 중요 결함 세 건을 고치고 재검토까지 통과했다 — 근거: 커밋 `9af028f`, 재검토 판정 "병합 준비 되었음"
- [확정] 검증이 초록불이다 — 근거: `just check` 종료코드 0 (Go 시험과 vet, Next.js 빌드, Vitest 13파일 211건, tsc, eslint)
- [확정] 기능이 브라우저에서 실제로 동작한다 — 근거: 서버 셋을 띄워 아홉 가지를 눈으로 확인했다. 그중 저장 경계(브라우저에 남는 것이 사용자가 친 글자 하나뿐)와 위치 권한을 묻지 않는 것이 이 기능의 핵심이다
- [확정] 카카오 장소 이름 검색은 한 검색어에 많아야 45곳, 한 번에 15곳, 마지막 페이지를 넘기면 오류 대신 마지막 페이지를 반복한다 — 근거: 2026-09-20 직접 조회, 그리고 브라우저 확인에서 재현(누적 60건이나 서로 다른 곳 45곳)
- [확정] 이 저장소의 화면 시험은 브라우저 없이 node에서 돈다 — 근거: `web/vitest.config.mts`

## 기각한 것
- [기각] `worktree.baseRef`를 `head`로 바꿔 분기 기준을 고치는 것 — 이유: 이 저장소의 앞으로 모든 worktree에 영향을 주는데, 이번 작업에서만 필요한 일이라 `git merge --ff-only main` 한 줄이면 끝난다
- [기각] 시한폭탄을 막으려고 `visits.ts`의 `now` 기본값을 없애 타입 검사로 강제하는 것 — 이유: 함수 넷의 인터페이스와 `app/page.tsx`의 모든 호출부를 바꾸게 되는데, 이번 작업과 한 줄도 이어지지 않는 변경이다
- [기각] 카카오 주소 검색(`address.json`)을 함께 붙이는 것 — 이유: `강남역`·`홍대입구`에 결과가 0건이고, 결과를 주는 경우에도 행정구역 중심점이라 애월읍 중심에서 곽지해수욕장까지 2,594m 떨어져 점심 반경 밖이다
- [기각] 장소 검색 결과를 분류 코드로 거르는 것 — 이유: 실측에서 `동궁과월지`·`첨성대`·`홍익대학교 정문`처럼 좋은 후보의 분류가 비어 있어, 거르면 잡음보다 신호가 더 많이 사라진다
- [기각] 빈 결과와 오류 화면에서 재시도할 때 로딩 중 시작 화면이 뜨는 것을 이번에 함께 고치는 것 — 이유: 이 가지 이전 코드를 열어 대조한 결과 이번 기능이 만든 동작이 아니라 앱 전역에 원래 있던 동작이다. 손대면 무관한 코드로 범위가 번진다

## 보류
- [보류] 사용자가 친 글자를 저장해도 되는지에 대한 카카오의 공식 답 — 필요한 것: 카카오 문의. 우리 해석은 "사용자 입력이지 카카오 응답이 아니다"이고 설계 문서 6절에 미확인 전제로 적어 두었다

## 다음에 할 일
- [x] writing-plans 스킬로 계획 작성 (커밋 `634417c`)
- [x] 실행 전 충돌 스캔. 두 건을 찾아 고침 (커밋 `36eb4c9`)
- [x] 열한 개 작업을 차례로 실행 (커밋 `56df4f7` ~ `f466cfe`)
- [x] 브라우저로 화면 흐름 확인, 발견한 결함 수정 (커밋 `c1e41fd`)
- [x] 브랜치 전체 리뷰와 수정, 재검토 (커밋 `9af028f`)
- [x] 설계 문서 그림의 옛 단추 이름 정리, 주석과 코드 맞추기
- [ ] 사람이 검토한 뒤 병합 여부를 결정한다. **이 작업은 사람만 할 수 있다**

**작업 진행의 자세한 기록은 `.superpowers/sdd/2026-09-20-move-search-location/progress.md`가 갖는다.**

## 주의
- **새 시험을 쓰면 그 방어를 일부러 껐을 때 실제로 실패하는지 확인한다.** 이 저장소에서 아무것도 지키지 않는 시험이 열세 번 나왔고, 이번 작업에서 세 번 더 나왔다.
- **시험할 수 있는 것은 전부 `web/lib/`의 순수 함수로 뺀다.** 화면 조각(`.tsx`)에는 시험이 붙지 않으므로 거기에 판단을 두면 아무도 지키지 않는다.
- **주석에 이유를 쓰기 전에 실제로 확인한다.** 이 저장소에서 주석의 근거가 코드와 어긋난 것이 여섯 번 나왔고, 이번 작업에서 세 번 더 나왔다.
- **서브에이전트에게 파일을 열게 할 때는 worktree 절대경로를 적는다.** 상대경로만 주면 원래 체크아웃을 읽고 엉뚱한 판정을 낸다.
- **Go 서버는 표준 라이브러리만 쓴다.** `api/go.mod`의 의존성 목록이 비어 있어야 한다.
- **저장 경계를 넘지 않는다.** 카카오가 저장을 허용한 것은 사용자가 직접 정한 장소의 장소식별값과 상호까지다. 좌표는 그 문구에 없다.
- 배포하지 않는다. 원격에 푸시하지 않는다. main에 머지하지 않는다.
